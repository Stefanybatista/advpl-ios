#include "protheus.ch"

//Crie um array(aArray) com três linhas. Em cada linha deve-se ter o nome do aluno e um
//outro array, pra guardar 4 notas de prova. Popule o array inteiro. Exiba o nome e as
//notas do segundo aluno.

 User function teste()

 local aArray:={}

 aAdd(aluno,{josian} )
 aAdd(aluno,
 
 )
 aAdd(aluno,)

















return 